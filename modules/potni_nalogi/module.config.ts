// Per-module config. Mirrors the registry entry; modules read their own config,
// never another module's.
export const config = {
  id: "potni_nalogi",
  name: "Potni nalogi",
  description:
    "Vnos stranke, relacije od–do, datuma in namena; km izračuna Google Maps; natisni potni nalog.",
};

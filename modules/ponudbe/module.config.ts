// Per-module config. Mirrors the registry entry; modules read their own config,
// never another module's.
export const config = {
  id: "ponudbe",
  name: "Ponudbe",
  description:
    "E-pošta (Outlook) → klasifikacija → ZOHO katalog → ponudba (PDF + osnutek e-pošte).",
};

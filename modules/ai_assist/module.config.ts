// Per-module config. Mirrors the registry entry; modules read their own config,
// never another module's.
export const config = {
  id: "ai_assist",
  name: "AI Assistant",
  description:
    "Ask a question; the AI answers and the Q&A is saved. The reference for /integrate-ai.",
};

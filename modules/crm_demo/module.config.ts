// Per-module config. Mirrors the registry entry; modules read their own config,
// never another module's.
export const config = {
  id: "crm_demo",
  name: "CRM (demo)",
  description:
    "Read-only contacts pulled from Intrix. The reference for /integrate-api.",
};

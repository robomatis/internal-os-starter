# /verify — potni_nalogi — 2026-06-30 13:45

- **Skill:** /verify
- **What ran:** lint + build (C: copy), /check-architecture, live dev server probes of the
  access guard. Module runs in **mock mode** (no `GOOGLE_MAPS_API_KEY`).

- **Result:**
  - ✓ `npm run lint` — No ESLint warnings or errors.
  - ✓ `npm run build` — green; `/m/[module]` compiles (23 kB). Only the expected,
    harmless Supabase `process.version` Edge Runtime warning.
  - ✓ `/check-architecture` — PASS: table `potni_nalogi_zapisi` prefixed; RLS + owner
    policy in the same migration; no cross-module imports; only `process.env.GOOGLE_MAPS_API_KEY`
    (server-only, not `NEXT_PUBLIC_`); registry + `core_modules` row + router case all present.
  - ✓ Server-side guard (unauthenticated): `/m/potni_nalogi`, `/m/admin`, `/m/invoice_ocr`,
    `/home` all return **307 → /login**; `/login` returns 200. A logged-out user cannot reach
    a module by URL.
  - ✓ Module registered in DB and reachable (route serves correctly after a clean `.next`).
  - ⏳ PENDING (needs owner login — password not yet set, and a member account):
    - signed-in **owner** walk: launcher shows *Potni nalogi*; create a nalog (Ljubljana → Maribor);
      km computed (mock); row saved in `potni_nalogi_zapisi`; print/document view renders.
    - **member** access: grant a member exactly one module; confirm launcher shows only that;
      an ungranted module URL is refused server-side; a member cannot grant modules.

- **Verdict:** PARTIAL — structural checks + server-side access guard PASS; the interactive
  signed-in owner/member walk is pending the owner setting their password and a member account.
- **Ref:** working tree (uncommitted); dev server on http://localhost:3000 (runs from C: copy).

## Notes / environment
- `npm install` / build / dev do not work on the Google Drive `G:` drive (filesystem write
  failures). A working copy lives at `C:\dev\internal-os-starter`; the dev server runs there
  via `.claude/launch.json` (cmd.exe `cd /d C:` wrapper). Edits on G: must be synced to C:
  before rebuilding.
- No `GOOGLE_MAPS_API_KEY` set → mock km (deterministic, badged "mock"). Add the key for real km.
